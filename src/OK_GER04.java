package comp;
public class A {
   public void    m() {
      int       i;
      boolean       b;
      System.out.print("" + (6));
(      i) = (1);
      while((      i<=5)) { 
         System.out.print("" + (         i) + (" "));
(         i) = (         i+1);
      }
(      b) = (false);
      while((      b!=true)) { 
         System.out.print("" + (6) + (" "));
(         b) = (         !(         b));
      }
   }
}
